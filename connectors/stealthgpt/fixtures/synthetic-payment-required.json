{
    "name": "synthetic-payment-required",
    "description": "402 when the account cannot be billed, zero-billed; serves every POST endpoint. Synthetic, from the published error reference.",
    "calls": [
        {
            "req": {
                "method": "POST",
                "url": "{{request.url}}"
            },
            "res": {
                "status": 402,
                "body": {
                    "message": "Payment required or payment method not set up"
                }
            }
        }
    ]
}
