{
    "name": "unauthorized-get",
    "description": "401 on a bad key (GET, no parameters). The vendor's non-2xx is DATA: the run completes, usage is empty, and the `{detail}` body passes through untouched.",
    "calls": [
        {
            "req": {
                "method": "GET",
                "url": "{{request.url}}"
            },
            "res": {
                "status": 401,
                "body": {
                    "detail": "Invalid or expired API key"
                }
            }
        }
    ]
}
