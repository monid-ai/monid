{
    "name": "insufficient-credits",
    "description": "The balance does not cover the worst case of the request (count x rate, checked before the call): 402 with the object detail every endpoint can answer with. Recorded live on /search/people 2026-09-23.",
    "calls": [
        {
            "req": {
                "method": "POST",
                "url": "{{request.url}}"
            },
            "res": {
                "status": 402,
                "body": {
                    "detail": {
                        "error": "Insufficient credits",
                        "available_credits": 997,
                        "required_credits": 1500,
                        "cost_per_result": 1.5
                    }
                }
            }
        }
    ]
}
