{
    "name": "synthetic-menu-unavailable",
    "description": "Synthetic upstream outage; preserve the provider error and settle zero usage.",
    "calls": [
        {
            "req": {
                "method": "GET",
                "url": "{{request.url}}"
            },
            "res": {
                "status": 503,
                "body": {
                    "error": "Service unavailable"
                }
            }
        }
    ]
}
