{
    "name": "routine-miss",
    "description": "An enrichment waterfall that found NOTHING: the item completes with an all-empty result (Clay's miss shape — a blank field value, equivalently {}). Six of the seven functions draw nothing here, so the derived fold settles to no credits at all; mobile-phone alone prices the miss and settles its miss_credits/miss_actions lines. Minimal shared chain (fixture strategy v2) — recorded live 2026-09-16 against clay#enrichment/mobile-phone (59 calls minimized to 3, run id normalized).",
    "calls": [
        {
            "req": { "method": "POST", "url": "{{request.url}}" },
            "res": {
                "status": 202,
                "body": {
                    "routine_run_id": "RUN1",
                    "status": "in_progress"
                }
            }
        },
        {
            "req": {
                "method": "GET",
                "url": "{{request.origin}}/public/v0/routines/run/RUN1/results?limit=100"
            },
            "res": {
                "status": 202,
                "body": {
                    "routine_run_id": "RUN1",
                    "status": "in_progress",
                    "total": 1,
                    "finished": 0
                }
            }
        },
        {
            "req": {
                "method": "GET",
                "url": "{{request.origin}}/public/v0/routines/run/RUN1/results?limit=100"
            },
            "res": {
                "status": 200,
                "body": {
                    "routine_run_id": "RUN1",
                    "status": "complete",
                    "total": 1,
                    "finished": 1,
                    "data": [
                        {
                            "id": "item-1",
                            "status": "complete",
                            "result": { "Mobile Phone": "" }
                        }
                    ]
                }
            }
        }
    ]
}
