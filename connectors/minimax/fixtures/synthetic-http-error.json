{
    "name": "synthetic-http-error",
    "description": "A real transport-level failure: HTTP 401 with the V2 OpenAI-style error body the /v2 surface uses. Relayed as data (zero usage) and digested by the provider's output.fromError. Synthetic.",
    "calls": [
        {
            "req": {
                "method": "POST",
                "url": "{{request.url}}"
            },
            "res": {
                "status": 401,
                "body": {
                    "type": "invalid_request_error",
                    "error": "invalid api key",
                    "request_id": "req_7d2a1f"
                }
            }
        }
    ]
}
